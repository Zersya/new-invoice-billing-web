export interface Client {
    name: string
    email: string
    tags: string[]
    verified_at: string
    $id: string
    $createdAt: string
    $updatedAt: string
    $permissions: string[]
    $collectionId: string
    $databaseId: string
}
